import React from "react"
import { Link } from "gatsby"

// {props.title}

const Logo = props => (
  <div className="site-logo">
    <Link to="/">Vierumäen kylä</Link>
  </div>
)

export default Logo
